name: ent-soft

services:
  mysql:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD is required}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-ent_soft}
      MYSQL_USER: ${MYSQL_USER:-ent_soft}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?MYSQL_PASSWORD is required}
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s
    networks:
      - ent_soft

  api:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      APP_ENV: ${APP_ENV:-prod}
      APP_DEBUG: ${APP_DEBUG:-0}
      APP_SECRET: ${APP_SECRET:?APP_SECRET is required}
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET:-${APP_SECRET}}
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_USER: ${MYSQL_USER:-ent_soft}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?MYSQL_PASSWORD is required}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-ent_soft}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD is required}
      DATABASE_URL: mysql://${MYSQL_USER:-ent_soft}:${MYSQL_PASSWORD}@mysql:3306/${MYSQL_DATABASE:-ent_soft}?serverVersion=8.0&charset=utf8mb4
      DEFAULT_URI: ${DEFAULT_URI:-http://localhost:8080}
      CORS_ALLOW_ORIGIN: ${CORS_ALLOW_ORIGIN:-'^https?://(.*)$'}
      FRANKFURTER_API_URL: ${FRANKFURTER_API_URL:-https://api.frankfurter.dev}
      FRANKFURTER_CACHE_TTL: ${FRANKFURTER_CACHE_TTL:-3600}
      RUN_MIGRATIONS: ${RUN_MIGRATIONS:-true}
      RUN_LOAD_FIXTURES: ${RUN_LOAD_FIXTURES:-false}
      SYMFONY_TRUSTED_PROXIES: ${SYMFONY_TRUSTED_PROXIES:-REMOTE_ADDR}
      SYMFONY_TRUSTED_HEADERS: ${SYMFONY_TRUSTED_HEADERS:-x-forwarded-for,x-forwarded-host,x-forwarded-proto}
    volumes:
      - api_uploads:/var/www/html/public/uploads
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/api/health"]
      interval: 30s
      timeout: 5s
      retries: 10
      start_period: 180s
    networks:
      - ent_soft

  web:
    build:
      context: .
      dockerfile: docker/frontend/Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-/api}
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    environment:
      API_UPSTREAM_HOST: ${API_UPSTREAM_HOST:-api}
      API_UPSTREAM_PORT: ${API_UPSTREAM_PORT:-80}
    networks:
      - ent_soft

networks:
  ent_soft:
    driver: bridge

volumes:
  mysql_data:
  api_uploads:
