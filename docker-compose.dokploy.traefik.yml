# Optionnel — labels Traefik manuels (alternative à l'onglet Domains Dokploy).
# Compose Path Dokploy : Symfony&Vue/docker-compose.dokploy.traefik.yml
# Variable obligatoire : APP_DOMAIN=cargo.example.com

include:
  - path: docker-compose.dokploy.yml

services:
  web:
    labels:
      - traefik.enable=true
      - traefik.http.routers.ent-soft-web.rule=Host(`${APP_DOMAIN:?APP_DOMAIN is required}`)
      - traefik.http.routers.ent-soft-web.entrypoints=websecure
      - traefik.http.routers.ent-soft-web.tls.certresolver=letsencrypt
      - traefik.http.services.ent-soft-web.loadbalancer.server.port=80
